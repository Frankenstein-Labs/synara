{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/apps/server/src/persistence/Migrations.ts"],
      "outputs": ["dist-electron/**"],
    },
    "dev": {
      "dependsOn": ["@cortex/cli#build"],
      "persistent": true,
    },
    "start": {
      "dependsOn": ["build", "@cortex/web#build", "@cortex/cli#build"],
      "cache": false,
      "persistent": true,
    },
    "smoke-test": {
      "dependsOn": ["build", "@cortex/web#build", "@cortex/cli#build"],
      "cache": false,
      "outputs": [],
    },
  },
}
